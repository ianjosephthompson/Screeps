const WORKER = 'worker';
const UPGRADER = 'upgrader';
const BUILDER = 'builder';
const REPAIRER = 'repairer';

module.exports = {
  WORKER: WORKER,
  UPGRADER: UPGRADER,
  BUILDER: BUILDER,
  REPAIRER: REPAIRER
};
